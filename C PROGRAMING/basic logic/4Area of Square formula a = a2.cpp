#include <stdio.h>

int main()
 {
    float a, area;
    printf("Enter the length of the cube: ");
    scanf("%f", &a);
    area = 6 * a * a;
    printf("The  area of the cube is: %.2f\n",area);
}
